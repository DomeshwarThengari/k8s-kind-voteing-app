apiVersion: v1
kind: Service
metadata:
  name: postgresdb
  namespace: voting-app
spec:
  type: ClusterIP
  ports:
  - name: "postgresdb-service"
    port: 5432
    targetPort: 5432
  selector:
    app: postgresdb